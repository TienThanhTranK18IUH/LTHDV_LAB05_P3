# Địa chỉ kết nối MongoDB (thay bằng URL của bạn)
MONGO_URI=mongodb://127.0.0.1:27017/supplier_product_db

# Secret cho session (nên đặt chuỗi ngẫu nhiên, ví dụ: "my_super_secret_123")
SESSION_SECRET=changeme

# Cổng chạy ứng dụng (mặc định 3000)
PORT=3000
